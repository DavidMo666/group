{
  "name": "advanced-finance-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla JavaScript personal finance tracker with Jest coverage evidence.",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "coverage:ci": "jest --coverage --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}